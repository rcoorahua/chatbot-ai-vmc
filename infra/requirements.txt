aws-cdk-lib==2.268.0
constructs>=10.0.0
# La alpha va SIEMPRE en lockstep con aws-cdk-lib (mismo numero + sufijo "aN") — subir una sin
# la otra rompe el import. PythonFunction bundlea con Docker corriendo.
aws-cdk.aws-lambda-python-alpha==2.268.0a0
pytest   # infra/tests/ (aws_cdk.assertions, no requiere Docker)
