# Entorno
.env
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# IDE
.vscode/
.idea/

# Docker (datos de DynamoDB Local)
docker/dynamodb-data/

# Chuleta personal de comandos (no se versiona)
my-usage.md
REFERENCIA/

# Salida de cdk synth (build)
infra/cdk.out/

# Contenido del Centro de Ayuda descargado y sus chunks. Se regeneran con
# `python -m scripts.helpcenter_fetch`, asi que versionarlos solo duplicaria contenido que ya
# es publico en el sitio de VMC y ensuciaria cada diff con texto regenerado.
data/helpcenter/*.md
data/helpcenter/chunks.json
!data/helpcenter/README.md
